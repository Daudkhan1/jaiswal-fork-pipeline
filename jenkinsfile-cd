pipeline {
    agent any 

    stages('Docker Deploy To Container') {
        steps {
            script {
                withDockerRegistry(credentialsId: 'dockerhub-id', toolName: 'docker') {
                    sh 'docker run -d --name shopping-cart -p 5000:5000 daudidrees/shopping-cart:latest'
                }
            }
        }
    }
}
